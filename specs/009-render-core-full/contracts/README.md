# Contracts: 009-render-core full

本 feature 对外 API 雏形（函数签名与类型）已写在 [plan.md](../plan.md) 末尾的 **「契约更新（API 雏形）」** 小节。

可直接将 plan.md 中该小节内容粘贴到 `specs/_contracts/009-rendercore-public-api.md` 的「API 雏形」小节（在 T0-contracts 分支上更新并推送）。

本目录无独立 OpenAPI/GraphQL 文件；C++ 引擎模块以契约文件中的 API 雏形为准。
