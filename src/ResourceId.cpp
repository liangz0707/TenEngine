/**
 * @file ResourceId.cpp
 * @brief ResourceId helpers (optional; ID is header-only for now).
 */
#include <te/resource/ResourceId.h>

namespace te {
namespace resource {
// ResourceId is currently a value type in the header; no extra implementation required.
}  // namespace resource
}  // namespace te
