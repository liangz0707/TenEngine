/**
 * @file Resource.cpp
 * @brief IResource implementation support (refcount etc. in implementation class).
 */
#include <te/resource/Resource.h>

namespace te {
namespace resource {
// IResource is interface-only; concrete refcount/Release in Loader-created implementation.
}  // namespace resource
}  // namespace te
