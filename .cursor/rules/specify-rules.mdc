# TenEngine-001-core Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-01-29

## Active Technologies
- C++17 + 无第三方库；仅 C++ 标准库（`<cstdlib>`, `<thread>`, `<mutex>`, `<atomic>`, `<filesystem>`, `<chrono>`, `<functional>` 等）与平台 API（Win32 / POSIX / dyld）。 (001-core-fullmodule)
- N/A（本模块不定义持久化存储；文件 I/O 仅平台能力）。 (001-core-fullmodule)
- C++17 + 001-Core（仅使用 `specs/_contracts/001-core-public-api.md` 声明的 Alloc/Free、Allocator、容器、字符串、日志等） (002-object-fullmodule-001)
- N/A（序列化缓冲由调用方管理；类型元数据在进程内存中，无持久化到文件） (002-object-fullmodule-001)
- C++17（Constitution：C++17 or later） + 001-Core（文件、内存、异步）、002-Object（序列化、反射、GUID）、028-Texture（契约依赖；类型化视图可选） (013-resource-fullmodule-002)
- 文件/包 I/O 经 001-Core；资源缓存为进程内内存（IResource* 按 ResourceId 索引） (013-resource-fullmodule-002)

- C++17 + 无（根模块）；可选 std 库：&lt;cstdlib&gt;, &lt;cstddef&gt;, &lt;mutex&gt;, &lt;atomic&gt;, &lt;cstdio&gt; 等 (001-core-minimal)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for C++17

## Code Style

C++17: Follow standard conventions

## Recent Changes
- 013-resource-fullmodule-002: Added C++17（Constitution：C++17 or later） + 001-Core（文件、内存、异步）、002-Object（序列化、反射、GUID）、028-Texture（契约依赖；类型化视图可选）
- 013-resource-fullmodule-002: Added C++17（Constitution：C++17 or later） + 001-Core（文件、内存、异步）、002-Object（序列化、反射、GUID）、028-Texture（契约依赖；类型化视图可选）
- 002-object-fullmodule-001: Added C++17 + 001-Core（仅使用 `specs/_contracts/001-core-public-api.md` 声明的 Alloc/Free、Allocator、容器、字符串、日志等）


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
