/**
 * @file containers.cpp
 * @brief No out-of-line definitions; Array/Map/String/UniquePtr/SharedPtr are type aliases to std types per contract (001-core-public-api.md).
 * Allocator support via template parameters. Comments in English.
 */

#include "te/core/containers.h"

namespace te {
namespace core {
  // Type aliases are defined in the header; this file exists for future non-inline helpers if needed.
}  // namespace core
}  // namespace te
